{{> menuadmin}}

<div class="jumbotron">
        <h2>Atalak</h2>
      </div>

     <div class="container">

<div class="row marketing">

        <div class="col-lg-8">
          <div class="col-lg-2">
          <form class="form-horizontal newsletterForm" role="form" action="/admin/atalakgehitu" method="POST">
        <input type="hidden" name="_csrf" value="{{_csrfToken}}">
          </div>
         <div class="form-group">
            <div class="col-sm-offset-3"> 
                <button type="submit" class="btn btn-default">Gehitu</button>
            </div>
        </div>
        </form> 

            <table class="table table-striped">
            <tr>
                    <th>Atal izena</th>
                    <th>Atal zenbakia</th>
                    <th></th>
                    <th></th>
                    <th></th>

                </tr>                               
            {{#each data}}
                  <tr>
                    <td>{{izenaAtala}}</td>
                    <td>{{zenbakiAtala}}</td>
                     <td>
                        <a class="a-inside edit" href="../admin/atalakeditatu/{{idAtalak}}">Aldatu</a>
                    </td>
                    <td>
                        <a class="a-inside delete" href="../admin/atalakezabatu/{{idAtalak}}">Ezabatu</a> 
                    </td>
                    <td>
                        <a class="a-inside insert" href="../admin/azpiAtalak/{{idAtalak}}">Azpi Atalak gehitu</a> 
                    </td>
                    
                </tr>
            {{/each}}
          </table>
        </div>

        
      </div>
      
</div>