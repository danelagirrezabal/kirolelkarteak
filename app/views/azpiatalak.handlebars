{{> menuadmin}}

<div class="jumbotron">
        <h2>AzpiAtalak</h2>
      </div>

     <div class="container">

<div class="row marketing">

        <div class="col-lg-8">
          <div class="col-lg-2">
          <form class="form-horizontal newsletterForm" role="form" action="/admin/azpiAtalakgehitu/{{idAtalak}}" method="POST">
        <input type="hidden" name="_csrf" value="{{_csrfToken}}">
          </div>
         <div class="form-group">
            <div class="col-sm-offset-3"> 
                <button type="submit" class="btn btn-default">Gehitu</button>
            </div>
        </div>
        </form> 


            <table class="table table-striped">
            <tr>
                    <th>Azpi Atal izena</th>
                    <th>Azpi Atal zenbakia</th>
                    <th></th>
                    <th></th>

                </tr>                               
            {{#each data}}
                  <tr>
                    <td>{{izenaAzpiAtala}}</td>
                    <td>{{zenbakiAzpiAtala}}</td>
                     <td>
                        <a class="a-inside edit" href="../azpiAtalakeditatu/{{idAzpiAtalak}}">Aldatu</a>
                    </td>
                    <td>
                        <a class="a-inside delete" href="../azpiAtalakezabatu/{{idAzpiAtalak}}">Ezabatu</a> 
                    </td>
                    <td>
                    <form class="form-horizontal newsletterForm" role="form" action="/admin/edukiakgehitu/{{idAzpiAtalak}}" method="POST">
                      <input type="hidden" name="_csrf" value="{{_csrfToken}}">
                       
                    <div class="form-group">
                      <div class="col-sm-offset-3"> 
                        <button type="submit" class="btn btn-default">Edukiak gehitu</button>
                      </div>
                    </div>
                    </form> 
                    </td>
                </tr>
            {{/each}}
          </table>
        </div>

        
      </div>
      
</div>