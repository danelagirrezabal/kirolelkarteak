{{#if admin}}
  {{> menuadmin}}
{{else}}
  {{> menu}}
{{/if}}

   <div class="jumbotron">
        <h2>Emaitzak sartu</h2>
      </div>
      </br></br>
       
      </br></br>
      <div class="row marketing">
        <div class="col-lg-12">
        <table class="table table-condensed">
            <tr>
                    <th  width="15%">Eguna</th>
                    <th>Maila</th>
                    <th>Txapelketa</th>
                    <th>A ekipoa</th>
                    <th>B ekipoa</th>
                    <th>Emaitza</th>
                    <th></th>
                </tr>          

            {{#each data}}
                  <tr class="active">
                    <td>{{dataPartidu}}</td>  
                    <td>{{izenaMaila}}</td> 
                    <td>{{txapelketaPartidu}}</td>  
                    <td>{{etxekoaPartidu}}</td>   
                    <td>{{kanpokoaPartidu}}</td>
                    <!--<td>{{emaitzaPartidu}}</td>-->
                    {{#if admin}}
                      <form class="form-horizontal newsletterForm" role="form" action="/admin/emaitzakgorde/{{idPartiduak}}" method="POST">
                    {{else}}
                      <form class="form-horizontal newsletterForm" role="form" action="/emaitzakgorde/{{idPartiduak}}" method="POST">
                    {{/if}}

                    <td><textarea name="emaitzaPartidu" rows="1" cols="5" class="form-control" required id="emaitzaPartidu" placeholder="0-0" value={{emaitzaPartidu}}>{{emaitzaPartidu}}</textarea></td>
                    <td>
                        <!--<a class="a-inside insert" href="/admin/emaitzakgorde/{{idPartiduak}}">Gorde</a> -->
                        <!--<a href="/admin/emaitzakgorde/{{idPartiduak}}" class="btn btn-info" role="button">Gorde</a>-->
                        <button type="submit" class="btn btn-default">Gorde</button>
                    </td>
                    </form>
                </tr>
            {{/each}}
          </table>

        </div>
      </div>

      