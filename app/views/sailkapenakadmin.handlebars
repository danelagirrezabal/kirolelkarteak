  {{> menuadmin}}

   <div class="jumbotron">
        <h2>Sailkapenak</h2>
   </div>
      </br></br>

      <div class="row marketing">

        <div class="col-lg-12">
        <table class="table table-condensed">
            <tr>
                    <th  width="15%">Taldearen izena</th>
                    <th>Maila</th>
                    <th>Sailkapena</th>

                </tr>                               
            {{#each data}}
                  <tr class="active">
                    <input type="hidden" name="idTaldeak" id="idTaldeak" value="{{idTaldeak}}">
                    <td>{{izenaTalde}}</td>  
                    <td>{{izenaMaila}}</td> 
                    <!--<td><a href='{{urlSailkapenTalde}}'>{{urlSailkapenTalde}}</a></td> -->

                    
                    <form class="form-horizontal newsletterForm" role="form" action="/admin/sailkapenakaldatu/{{idTaldeak}}" method="POST">
                    <td>
                    <textarea name="urlSailkapenTalde" rows="1" cols="40" class="form-control" required id="urlSailkapenTalde" value={{urlSailkapenTalde}}>{{urlSailkapenTalde}}</textarea>
                    </td>

                    <td><button type="submit" class="btn btn-default">Gorde</button></td>

                    </form>
                    
                </tr>
            {{/each}}
          </table>
        </div>

        <div class="col-lg-10">
          <p align='center'>
          <a href="/admin/sailkapenakaldatu/" class="btn btn-primary btn-bg"  role="button">ALDAKETAK GORDE</a>
          </p>
        </div>
</div>
